# Documentation

Current setup and structure:

- [Project structure](../PROJECT_STRUCTURE.md)
- [Backend and database setup](../backend/README.md)

The `maps/`, `treasury/`, and `project/` folders contain historical feature
guides. Their old file paths and screenshots are retained for context; use the
current structure documents above when changing the application.
